document.addEventListener("keypress",event =>{
    const user=document.getElementById('username').value;
    console.log(user);
});
/*key board events are three types
keypress->it is used to enter a text is one step is back
keyup->it is used to enter a text is same speed
keydown->it is work like as keypress  */